<section class="{{cssClass}} sheet-form-layout" autocomplete="off">
  <header class="sheet-header">
    <div class="header-content">
      <img class="profile-img" src="{{actor.img}}" data-action="editImage" title="{{actor.name}}" />

      <div class="header-info">
        <h1><input name="name" type="text" value="{{actor.name}}" placeholder="Nom du Caïd" /></h1>

        <div class="confiance-controls">
          <div class="form-group-inline">
            <label>Confiance</label>
            <select name="system.confiance" data-dtype="String">
              {{selectOptions config.levelsList selected=system.confiance valueAttr="value" nameAttr="value" labelAttr="label"}}
            </select>
          </div>

          <div class="roll-controls">
            <select class="roll-input" name="selectRollInput" data-dtype="Number">
                  {{selectOptions config.levelsList selected=selectRollInput valueAttr="value" nameAttr="value" labelAttr="label"}}
            </select>
            <button type="button" data-action="rollSimpleMJ">Lancer!</button>
          </div>
        </div>
      </div>

    </div>
  </header>

  <nav class="sheet-tabs tabs" data-group="primary">
    <a class="item{{#if (eq activeTab 'biodata')}} active{{/if}}" data-action="changeTab" data-tab="biodata">Biographie</a>
    <a class="item{{#if (eq activeTab 'regles')}} active{{/if}}" data-action="changeTab" data-tab="regles">Règles</a>
    <a class="item{{#if (eq activeTab 'trait')}} active{{/if}}" data-action="changeTab" data-tab="trait">Traits</a>
  </nav>

  <section class="sheet-body">
    {{!-- Biographie Tab --}}
    <div class="tab{{#if (eq activeTab 'biodata')}} active{{/if}}" data-tab="biodata">
      <div class="question-section">
        <h3>Honnêtenement, comment il a éliminé la concurrence, le caïd ?</h3>
        <textarea name="system.reponseCaid01" rows="5">{{actor.system.reponseCaid01}}</textarea>
      </div>

      <div class="question-section">
        <h3>C'est quel genre de chef ?</h3>
        <textarea name="system.reponseCaid02" rows="5">{{actor.system.reponseCaid02}}</textarea>
      </div>

      <div class="question-section">
        <h3>Et avec les gros bonnets, il en est où ?</h3>
        <textarea name="system.reponseCaid03" rows="5">{{actor.system.reponseCaid03}}</textarea>
      </div>
    </div>

    {{!-- Règles Tab --}}
    <div class="tab{{#if (eq activeTab 'regles')}} active{{/if}}" data-tab="regles">
      <div class="rules-section">
        <h3>Confiance du Caïd</h3>
        <table class="rules-table">
          <thead>
            <tr>
              <th>Score</th>
              <th>Confiance</th>
              <th>Popularité</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>(++)</td>
              <td>Efficaces. A surveiller</td>
              <td>Ils iront loin (++)</td>
            </tr>
            <tr>
              <td>(+)</td>
              <td>Des durs</td>
              <td>Un exemple à suivre (+)</td>
            </tr>
            <tr>
              <td>(0)</td>
              <td>Le ventre mou</td>
              <td></td>
            </tr>
            <tr>
              <td>(-)</td>
              <td>Pas doués</td>
              <td>Vaut mieux pas traîner avec eux (-)</td>
            </tr>
            <tr>
              <td>(--)</td>
              <td>Sacrifiables et inutiles</td>
              <td>Des minables (--)</td>
            </tr>
          </tbody>
        </table>

        <h3>Jet pour obtenir des Bonus ?</h3>
        <table class="rules-table">
          <thead>
            <tr>
              <th>D6</th>
              <th>Réponse</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>1</td>
              <td>Un Trait (-) et une dette (--)</td>
            </tr>
            <tr>
              <td>2</td>
              <td>Un Trait (0) et une dette envers le caïd (--)</td>
            </tr>
            <tr>
              <td>3 - 4</td>
              <td>Un Trait (-) et un trait (+) et une dette (-)</td>
            </tr>
            <tr>
              <td>5</td>
              <td>Un Trait (+) et une dette (-)</td>
            </tr>
            <tr>
              <td>6</td>
              <td>Un Trait (++) et pas de dette</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    {{!-- Traits Tab --}}
    <div class="tab{{#if (eq activeTab 'trait')}} active{{/if}}" data-tab="trait">
      <div class="traits-section">
        <div class="traits-header">
          <h3>Traits</h3>
          <button type="button" data-action="createItem" data-item-type="trait" title="Créer un trait">
            <i class="fas fa-plus"></i> Ajouter un trait
          </button>
        </div>

        <ul class="item-list">
          {{#each traits as |trait|}}
          <li class="item" data-item-id="{{trait._id}}">
            <img class="item-img" src="{{trait.img}}" data-action="editItem" data-item-id="{{trait._id}}" />
            <span class="item-name">{{trait.name}}</span>
            <span class="item-origin">{{getTraitType trait.system.type}}</span>
            <button type="button" class="item-edit" data-action="editItem" data-item-id="{{trait._id}}" title="Éditer">
              <i class="fas fa-edit"></i>
            </button>
            <button type="button" class="item-delete" data-action="deleteItem" data-item-id="{{trait._id}}" title="Supprimer">
              <i class="fas fa-trash"></i>
            </button>
          </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </section>
</section>
